In the full screen carousel there should be buttons to switch the image
Compact the current pictures and add a script to compact images to some better format (add change all .png mentions)
Unify all scripts by adding a new script called "generate.py" that will just call all other scripts